package uk.ac.bham.codeclassroom.dto;

import java.time.LocalDate;
import java.math.BigDecimal;
import java.util.List;
import java.util.Set;
import java.util.Collection;

/**
 * A DTO for the {@link uk.ac.bham.codeclassroom.model.{{className}}} entity.
 */
public class {{className}}DTO{{#requiresExtends}} extends {{parentClassName}}DTO{{/requiresExtends}} {

    private Long id;

    {{#fields}}
    private {{type}} {{name}};
    {{/fields}}

    public {{className}}DTO() {
    }

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    {{#fields}}
    public {{type}} get{{capitalizedName}}() {
        return {{name}};
    }

    public void set{{capitalizedName}}({{type}} {{name}}) {
        this.{{name}} = {{name}};
    }
    {{/fields}}
}
