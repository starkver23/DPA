package uk.ac.bham.codeclassroom.mapper;

import org.springframework.stereotype.Component;
import uk.ac.bham.codeclassroom.dto.{{className}}DTO;
import uk.ac.bham.codeclassroom.model.{{className}};

/**
 * Mapper for the entity {@link {{className}}} and its DTO {@link {{className}}DTO}.
 */
@Component
public class {{className}}Mapper {

    /**
     * Maps an entity to its DTO representation.
     */
    public {{className}}DTO toDto({{className}} entity) {
        if (entity == null) {
            return null;
        }
        // TODO: Implement field mappings
        {{className}}DTO dto = new {{className}}DTO();
        dto.setId(entity.getId());
        return dto;
    }

    /**
     * Maps a DTO to its entity representation.
     */
    public {{className}} toEntity({{className}}DTO dto) {
        if (dto == null) {
            return null;
        }
        // TODO: Implement field mappings
        {{className}} entity = new {{className}}();
        entity.setId(dto.getId());
        return entity;
    }
}
